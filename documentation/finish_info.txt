

------- Build finished: Now you can do ... -------

1) make install
  To install the libraries on your system

2) make install_toolbox
  To install the scilab5 toolbox


.) Within documentation/List_of_Module_Blocks.txt you will find a list of 
   blocks available through the modules

.) the command libdyn_generic_exec is used to execute schematics.
   See libdyn_generic_exec --help for usage.

.) the hart (hart.sf.net) module can be found within
   hart/hart_ortd

.) to run a demonstration:
   # cd examples
   # sh sh_runoscillator
   or
   # sh sh_runoscillator_realtime

.) have a look at INTRODUCTION and REAME files
